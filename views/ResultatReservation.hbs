{{!--  <link rel="stylesheet" href="../static/css/resultat.css"> --}}
<div class="contenu">
     {{#each donnees}}
    <h1><em>Information sur la réservation n°{{this.id_reservation}} </em></h1>
    {{/each}}
    <div class="affichage">
        <table id="tableR">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom</th>
                    <th>Âge</th>
                    <th>Chaise</th>
                    <th>Horaire</th>
                </tr>
            </thead>

            <tbody>
                {{#each donnees}}
                <tr class="result">
                    <td>{{this.id_reservation}}</td>
                    <td>{{this.nom}}</td>
                    <td>{{this.age}}</td>
                    <td>{{this.nb_chaise}}</td>
                    <td>{{this.horaireS}}</td>
                    <td><a href="/reservation/deleteR/{{this.id_reservation}}" id="supprimer">Supprimer</a></td>
                </tr>
                {{/each}}
            </tbody>

        </table>
    </div>
</div> 